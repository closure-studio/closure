# Theme Extensions

The root `tamagui.config.ts` is the source of truth for colors, spacing, typography, radii, media queries, and animation names. Platform animation drivers live here: Web resolves `animations.ts` to the CSS driver, while iOS and Android resolve `animations.native.ts` to the React Native driver.
