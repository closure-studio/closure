# Theme Extensions

The root `tamagui.config.ts` is the source of truth for colors, spacing, typography, radii, media queries, and animations. Use this directory only for future platform adapters that cannot live in Tamagui configuration.
