# Global Types

Ambient and module declarations belong here. Do not place domain models, business inputs, or aliases for schema-derived types in this directory; Valibot schemas and their inferred types live in `schemas/<domain>/`.
