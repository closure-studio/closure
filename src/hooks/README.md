# Global Hooks

Only hooks reused by multiple features belong here. Feature-specific hooks stay in `features/<feature>/hooks/`. Prefer Tamagui theme and media APIs over duplicate theme or breakpoint hooks.
