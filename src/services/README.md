# Services

Use this directory for cross-feature application services and orchestration. Feature-specific endpoint calls belong in `features/<feature>/api/`; low-level third-party client initialization belongs in `src/lib/`.
